import {connection,gather,encode,decode,stateReceiver} from './peer.js?v=graphics124';
import {accept} from './overlay-boot.js?v=graphics124';
const panel=document.createElement('div');panel.style.cssText='position:fixed;inset:30px;max-width:640px;max-height:600px;padding:24px;background:#142230;color:white;font:18px Arial;z-index:999;overflow:auto';panel.innerHTML='<h2>Connect this OBS output</h2><p>Paste the offer from the control panel’s Pair OBS button.</p><textarea id="offer" aria-label="Offer code"></textarea><button id="answer">Create answer</button><p>Copy this answer back into the control panel and click Connect.</p><textarea id="answer-code" aria-label="Answer code" readonly></textarea><p id="status">Waiting for pairing</p><p>This form disappears after connection. Re-pair after refreshing either page. Both devices must be on the same local network; there is no relay server.</p>';document.body.append(panel);panel.querySelectorAll('textarea').forEach(e=>e.style.cssText='display:block;width:95%;height:80px');let pc;
panel.querySelector('#answer').onclick=async()=>{try{pc?.close();pc=connection();pc.ondatachannel=({channel})=>{stateReceiver(channel,accept);channel.onopen=()=>panel.hidden=true;channel.onclose=()=>{panel.hidden=false;panel.querySelector('#status').textContent='Disconnected — re-pair before going live';};};await pc.setRemoteDescription(decode(panel.querySelector('#offer').value));await pc.setLocalDescription(await pc.createAnswer());await gather(pc);panel.querySelector('#answer-code').value=encode(pc.localDescription);panel.querySelector('#status').textContent='Answer ready';}catch(e){panel.querySelector('#status').textContent=e.message;}};
